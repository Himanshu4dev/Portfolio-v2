{
  "version": 2,
  "builds": [
    { "src": "next.config.mjs", "use": "@vercel/next" }
  ],
  "env": {
    "ADMIN_TOKEN": "@admin_token"
  },
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/$1" }
  ]
}
